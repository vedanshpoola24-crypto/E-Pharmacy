# MedStore Frontend

Static frontend for Vercel or Netlify.

Set the backend URL before deployment by editing `js/config.js`:

```js
window.MEDSTORE_API_URL = "https://your-render-api.onrender.com";
```

Local test:

```bash
python -m http.server 3000
```
